import { InvertColorDirective } from './invert-color.directive';

describe('InvertColorDirective', () => {
  it('should create an instance', () => {
    const directive = new InvertColorDirective();
    expect(directive).toBeTruthy();
  });
});
